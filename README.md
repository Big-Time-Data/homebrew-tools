# homebrew-tools

Homebrew tools by BTD.

## mcpq

Let an LLM query a customer's warehouse and find the context it needs.

```bash
brew install Big-Time-Data/tools/mcpq
```

Homebrew asks you to trust the tap on first install. Run the command it prints, then install again.

Upgrade:

```bash
brew upgrade mcpq
```
